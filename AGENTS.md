## 기본 지침 (for Korean users)

- 본 사용자는 **한국어 사용자**입니다.  
- 절대 `~/Desktop/work/codex` 디렉터리 **외부에서 작업을 수행하지 마세요.**  
- 코드 작업 시에는 **MCP Server `context7`**을 반드시 참고하여, 프로젝트에 **적합한 라이브러리와 종속성(dependency)** 을 선택하도록 하세요.  
- 모든 명령어와 스크립트는 이 디렉터리 내에서만 실행되어야 합니다.